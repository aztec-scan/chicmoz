apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: explorer-ui-v2-testnet-label
  name: explorer-ui-v2-testnet-deployment
  namespace: chicmoz
spec:
  replicas: 1
  selector:
    matchLabels:
      app: explorer-ui-v2-testnet-label
  template:
    metadata:
      labels:
        app: explorer-ui-v2-testnet-label
    spec:
      containers:
        - image: explorer-ui-v2-testnet:latest
          name: explorer-ui-v2-testnet
          resources:
            limits:
              memory: 300Mi
              cpu: 500m
          ports:
            - name: http-app-port
              containerPort: 80
              protocol: TCP
