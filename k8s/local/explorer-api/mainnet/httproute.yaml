apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: explorer-api-mainnet
  namespace: chicmoz
  labels:
    app.kubernetes.io/name: explorer-api
    app.kubernetes.io/part-of: chicmoz-local
spec:
  parentRefs:
    - name: chicmoz-gateway
      sectionName: api-mainnet
    - name: chicmoz-gateway
      sectionName: api-mainnet-remote
  hostnames:
    - api.mainnet.chicmoz.localhost
    - api.remote.chicmoz.localhost
  rules:
    - matches:
        - path:
            type: RegularExpression
            value: /v1/([^/]+)/(.*)
      filters:
        - type: ExtensionRef
          extensionRef:
            group: gateway.envoyproxy.io
            kind: HTTPRouteFilter
            name: explorer-api-mainnet-rewrite
      backendRefs:
        - name: explorer-api-mainnet-service
          port: 80
