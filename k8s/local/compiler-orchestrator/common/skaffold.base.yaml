apiVersion: skaffold/v4beta6
kind: Config
requires:
  - path: ../../common/skaffold.chicmoz-base-image.yaml
build:
  artifacts:
    - image: compiler-orchestrator
      context: ../../../../services/compiler-orchestrator
      docker:
        dockerfile: Dockerfile
      requires:
        - image: chicmoz-base
          alias: BASE
