apiVersion: v1
kind: Service
metadata:
  name: explorer-api-mainnet-service
  namespace: chicmoz-prod
spec:
  selector:
    app: explorer-api-mainnet-label
  ports:
    - name: http-app
      protocol: TCP
      port: 80
      targetPort: http-app-port
