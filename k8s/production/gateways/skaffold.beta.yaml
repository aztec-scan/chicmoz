apiVersion: skaffold/v4beta6
kind: Config
metadata:
  name: gateways-beta
manifests:
  rawYaml:
    - ./beta/gateway.yaml
    - ./beta/clienttrafficpolicy.yaml
    - ./beta/certificates.yaml
    - ./beta/certificates-primary.yaml
deploy:
  kubectl:
    flags:
      apply: ["--force"]
