apiVersion: skaffold/v4beta6
kind: Config
metadata:
  name: gateways-devnet
manifests:
  rawYaml:
    - ./gateway.yaml
    - ./devnet/certificates.yaml
deploy:
  kubectl:
    flags:
      apply: ["--force"]
