apiVersion: v1
kind: Service
metadata:
  name: explorer-api-testnet-external
  namespace: chicmoz-staging
spec:
  type: NodePort
  selector:
    app: explorer-api-testnet-label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30081
