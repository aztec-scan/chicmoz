apiVersion: v1
kind: Service
metadata:
  name: websocket-event-publisher-testnet-external
  namespace: chicmoz-staging
spec:
  type: NodePort
  selector:
    app: websocket-event-publisher-testnet-label
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000  # Your WebSocket service port
      nodePort: 30084
