apiVersion: v1
kind: Service
metadata:
  name: explorer-ui
  labels:
    app.kubernetes.io/name: explorer-ui
    app.kubernetes.io/part-of: chicmoz-prod
spec:
  selector:
    app: explorer-ui
  ports:
    - name: http-app
      protocol: TCP
      port: 80
      targetPort: http-app-port
